extends layout

block content
  //- h1= title
  //- p Welcome to #{title}

  .container-fluid
    .row
      .col-xs-12.col-sm-8.col-md-6.col-lg-6.col-xs-offset-0.col-sm-offset-2.col-md-offset-3.col-lg-offset-3.sidebar

        .nav.nav-pills.nav-stacked
          
            if reqUser
              li.active
                a(href='/auth/logout')
                  span.glyphicon.glyphicon-log-out(aria-hidden='true')
                  |  Logout
            else
              li.active
                a(href='/auth/login')
                  span.glyphicon.glyphicon-log-in(aria-hidden='true')
                  |  Login
              li
                a(href='/auth/facebook') Facebook
