
if error.message || error.status
  h1
    if error.status
      = error.status + '. '
    if error.message
      = error.message

if error.uri
  h3
    = error.uri

if error.stack
  pre
    = error.stack
    