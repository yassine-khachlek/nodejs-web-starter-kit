extends layout

block content
  //- h1= title
  //- p Welcome to #{title}

  .container
    .row
      .col-sm-3.col-md-2.sidebar
        ul.nav.nav-sidebar
          each val, index in routes
            li
              a(href='/'+val)=val.charAt(0).toUpperCase() + val.slice(1)
